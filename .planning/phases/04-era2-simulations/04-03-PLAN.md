---
wave: 1
depends_on: []
files_modified:
  - Episodio4/stops/012-newton-laws/sim.js
  - Episodio4/stops/013-newton-gravity/sim.js
autonomous: true
objective: >
  Build sim.js for Newton's Laws (F=ma block on surface with friction, inertia demo,
  velocity graph, wall reaction) and Newton's Universal Gravitation (cannon shot with
  RK4 gravity, projectile-to-orbit transition at 7.9 km/s, escape at 11.2 km/s).
must_haves:
  - Newton laws sim: "Apply Force" toggle applies/releases force; block keeps moving after release (1st law)
  - Newton laws sim: F=ma readout shows live acceleration for current force/mass
  - Newton laws sim: friction slider (None/Low/Medium/High) affects deceleration after release
  - Newton laws sim: wall reaction arrow appears on bounce (3rd law visual)
  - Newton laws sim: velocity over time mini graph shows constant acceleration, then friction deceleration
  - Newton gravity sim: RK4 integration of F = GMm/r² in km/km/s units
  - Newton gravity sim: launch speed slider 1–12 km/s
  - Newton gravity sim: outcome correctly detected: landed / orbiting / escaped
  - Newton gravity sim: trail color changes by outcome type
  - Both sims: window.SimAPI = { start, pause, reset, destroy }
---

<objective>
Newton laws: inertia must feel tangible — the block glides after force release, friction
gradually brings it to rest. The user controls the trade-off between them. Newton gravity:
the moment the cannonball transitions from parabola to orbit is the main insight — it
must be visually unmistakable.
</objective>

<tasks>

<task id="04-03-T1">
  <name>Write Newton Laws sim.js</name>
  <files>Episodio4/stops/012-newton-laws/sim.js</files>
  <action>
    IIFE. Block (64×48 px) on floor with texture. Sliders: force (0–50N), mass (1–20kg),
    friction (4 levels: 0, 0.05, 0.15, 0.35 µ values).
    "Apply Force" button toggles force application. While force applied: F_net = F_app - µmg.
    After release: only friction decelerates. Wall at right: block bounces (e=0.5).
    Left boundary: block can't leave canvas.
    Velocity history array (200 samples) → mini line graph.
    F=ma panel: live values for F_net, m, a, v.
    Reaction arrow: displayed for 30 frames after wall bounce.
    Status text: changes to explain what's happening.
  </action>
  <verify>
    With friction=None and force released: block moves at constant speed indefinitely (1st law).
    F=ma panel: a = 0 when force=0 and friction=None.
    Wall bounce: reaction arrow appears briefly.
  </verify>
  <done>Newton laws block demonstrates inertia, F=ma, and reaction force visually.</done>
</task>

<task id="04-03-T2">
  <name>Write Newton Gravity (cannon) sim.js</name>
  <files>Episodio4/stops/013-newton-gravity/sim.js</files>
  <action>
    IIFE. Earth circle at canvas center (earthRadius = min(W,H)*0.28 px).
    Cannon at top-right of Earth (angle -π/4, altitude 200km above surface).
    Launch direction: perpendicular to radius (horizontal at cannon position).
    RK4: GM = 9.8 × R_earth² × 1e6 m³/s², converted to km/s² for km-unit physics.
    30 sub-steps per frame. Simulation seconds per wall-second: ~60.
    Trail: store (x,y) in km. Convert to canvas: cx + km_to_px(x).
    Outcome detection:
      r < R_earth + 10 → "landed" (stop sim)
      r > R_earth * 15 → "escape" (stop sim)
      simT > 5000 && !outcome → "orbit" (keep running, set label)
    Speed info panel: launch speed vs orbital (7.9) and escape (11.2) thresholds.
  </action>
  <verify>
    At speed=7.9 km/s: orbit outcome. Trail forms closed loop.
    At speed=5.0 km/s: landed outcome. Trail hits Earth surface.
    At speed=12.0 km/s: escape outcome. Trail leaves canvas.
  </verify>
  <done>Newton cannon shows all three outcomes correctly at the right speed thresholds.</done>
</task>

</tasks>
