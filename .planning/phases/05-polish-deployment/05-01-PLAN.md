---
wave: 1
depends_on: []
files_modified:
  - Episodio4/README.md
  - Episodio4/assets/css/base.css
  - Episodio4/assets/css/components.css
  - Episodio4/assets/css/simulation.css
autonomous: true
objective: >
  Accessibility audit and fix pass. Write README.md with local dev, add-a-stop guide,
  and deployment instructions. Verify all stop pages have correct meta descriptions.
  Check color contrast ratios. Confirm prefers-reduced-motion in JS loops.
must_haves:
  - README.md present at Episodio4/README.md with local dev command
  - README.md has "How to add a new stop" section with numbered steps
  - README.md has "Deploy to GitHub Pages" section
  - All 50 stop pages have non-empty <meta name="description"> tags
  - All interactive controls (sliders, buttons) have aria-label attributes
  - prefers-reduced-motion: JS sims check window.matchMedia and skip animation if set
---

<objective>
Production readiness pass. No new features — fix what exists to meet the quality bar
for a public GitHub Pages site. README is the developer entry point.
</objective>

<tasks>

<task id="05-01-T1">
  <name>Write README.md</name>
  <files>Episodio4/README.md</files>
  <action>
    Write: project overview (1 paragraph), local development (python3 -m http.server 8765
    --directory Episodio4), how to add a new stop (5 steps: create directory, write index.html
    following the template, write sim.js exposing SimAPI, add to stops.json, test navigation chain),
    deploy to GitHub Pages (gh-pages branch or custom directory setting), tech stack table.
  </action>
  <verify>README.md > 80 lines. Sections present: Overview, Local Development, Adding a Stop, Deployment, Tech Stack.</verify>
  <done>README.md present and complete at Episodio4/README.md.</done>
</task>

<task id="05-01-T2">
  <name>Accessibility fixes</name>
  <files>Various stop HTML files, CSS files</files>
  <action>
    Verify all sliders have aria-label attributes (check 3 random stop pages).
    Verify all sim buttons have descriptive text (not just icons).
    Check that .stop-card links have descriptive text (not just "click here").
    Add aria-live="polite" to #sim-status elements so screen readers announce updates.
    Verify .sr-only class is used for visually-hidden labels where needed.
  </action>
  <verify>
    Spot-check: grep -r 'aria-label' Episodio4/stops/004-aristotle-motion/index.html returns results.
    Spot-check: sim-status elements have aria-live attribute.
  </verify>
  <done>Accessibility audit pass complete. Major WCAG A violations resolved.</done>
</task>

<task id="05-01-T3">
  <name>Performance check: remove unused vendor lib loads</name>
  <files>Episodio4/stops/*/index.html</files>
  <action>
    Check which stop pages load p5.min.js or matter.min.js but don't actually use them
    (v1.0 sims are all vanilla Canvas, neither library is used).
    Remove vendor script tags from all stop pages if not used.
    Confirm p5.min.js and matter.min.js remain in vendor/ dir (for Phase 3+ use).
  </action>
  <verify>
    grep -r 'p5.min.js' Episodio4/stops/ | wc -l returns 0 if no stop pages load p5.
  </verify>
  <done>Vendor libs not loaded on any current stop page (saved ~1.1MB per page load).</done>
</task>

</tasks>
