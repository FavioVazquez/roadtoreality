---
wave: 2
depends_on: [01-01]
files_modified:
  - Episodio4/assets/js/progress.js
  - Episodio4/assets/js/stop-shell.js
  - Episodio4/assets/js/nav.js
  - Episodio4/index.html
autonomous: true
objective: >
  Build all shared JavaScript modules (progress.js, stop-shell.js, nav.js) and the
  landing page (index.html). The landing page must render the era timeline, card grid
  (from stops.json), animated starfield, and sticky era tabs. All JS as plain IIFEs.
must_haves:
  - progress.js exposes window.PhysicsProgress with markVisited, isVisited, getVisitedCount, getAllVisited
  - stop-shell.js reads #stop-config JSON, renders breadcrumb and stop-nav via DOM, wires IntersectionObserver to SimAPI
  - nav.js fetches assets/data/stops.json, renders era sections + card grid, shows visited badges
  - index.html has: hero, era timeline, sticky era tabs, #era-sections, #progress-bar, starfield canvas
  - nav.js shows a human-readable error if stops.json fetch fails
  - stop-shell.js play/pause/reset buttons call SimAPI correctly
---

<objective>
Build the shared JavaScript layer and the landing page. All JS must be plain IIFEs
with no build step. The landing page is the entry point visitors see first — it must
render the full stop card grid from stops.json dynamically.
</objective>

<tasks>

<task id="01-02-T1">
  <name>Write progress.js</name>
  <files>Episodio4/assets/js/progress.js</files>
  <action>
    IIFE exposing window.PhysicsProgress = { markVisited(id), isVisited(id),
    getVisitedCount(), getAllVisited() }. Uses localStorage key 'physics-history-progress'.
    Store: JSON object mapping stopId → timestamp. All localStorage access in try/catch.
  </action>
  <verify>window.PhysicsProgress exists with all 4 methods. localStorage key documented.</verify>
  <done>progress.js loads without errors in browser console.</done>
</task>

<task id="01-02-T2">
  <name>Write stop-shell.js</name>
  <files>Episodio4/assets/js/stop-shell.js</files>
  <action>
    IIFE. On DOMContentLoaded: read #stop-config JSON script tag, render breadcrumb into
    #stop-breadcrumb, render prev/next into #stop-nav, set #stop-counter text.
    Register IntersectionObserver on #sim-mount (threshold 0.15) → calls SimAPI.start/pause.
    Wire #sim-play-btn click → toggle SimAPI.start/pause, update button text + dot state.
    Wire #sim-reset-btn click → SimAPI.reset, reset button state.
    Call PhysicsProgress.markVisited(config.id).
  </action>
  <verify>Breadcrumb renders links. Prev/next links have correct hrefs. Observer registered.</verify>
  <done>stop-shell.js loads and wires all controls without errors.</done>
</task>

<task id="01-02-T3">
  <name>Write nav.js</name>
  <files>Episodio4/assets/js/nav.js</files>
  <action>
    IIFE. fetch('assets/data/stops.json') → group stops by era → render into #era-timeline
    and #era-sections. ERA_ORDER = ['ancient','revolution','classical','modern','contemporary'].
    Each stop card: anchor with data-era, .stop-card, visited badge class if isVisited.
    Stub stops: div.stop-card--stub, not clickable. Render progress bar into #progress-bar.
    On fetch error: show human-readable message with python3 -m http.server hint.
  </action>
  <verify>Card grid renders all 50 stops. Stub stops have .stop-card--stub class. Error shown on fetch fail.</verify>
  <done>nav.js renders full card grid from stops.json without console errors.</done>
</task>

<task id="01-02-T4">
  <name>Write index.html (landing page)</name>
  <files>Episodio4/index.html</files>
  <action>
    Full HTML with: site-header (logo + tagline + visit counter), hero section (eyebrow,
    h1 with em for gold highlight, subtitle, stats, progress bar slot), sticky era-tabs nav
    (5 era tabs), era-timeline div (#era-timeline), main #era-sections, site-footer.
    Starfield: fixed canvas#star-canvas with inline JS (particle system, respects
    prefers-reduced-motion). Scroll-spy for active era tab.
    Load order: progress.js → nav.js → inline starfield script.
  </action>
  <verify>
    File > 150 lines. All 5 era tabs present. #era-sections and #era-timeline divs present.
    Starfield canvas exists. Scripts loaded in correct order.
  </verify>
  <done>index.html served by python3 -m http.server renders correctly with card grid.</done>
</task>

</tasks>
