---
wave: 1
depends_on: []
files_modified:
  - Episodio4/assets/data/stops.json
autonomous: true
objective: >
  Create stops.json with all 50 stops: full metadata (id, slug, order, title, subtitle,
  era, year, yearLabel, scientist, isStub, description, path, prev, next, prevTitle,
  nextTitle). Navigation chain forms one unbroken prev/next sequence 1-50.
must_haves:
  - stops.json is valid JSON with a top-level "stops" array of exactly 50 objects
  - All 5 era values used: ancient, revolution, classical, modern, contemporary
  - stops[0].prev === null, stops[49].next === null
  - For all i in 0..48: stops[i].next === stops[i+1].id
  - 12 stops have isStub=false (001, 003-013); all others isStub=true
  - Every stop has non-empty description (2-3 sentences, accurate physics history)
---

<objective>
Create the single source of truth for all stop metadata. Every downstream artifact
(nav.js card grid, stop-shell.js breadcrumbs, stub generator) reads from this file.
Accuracy of physics history content matters — dates and scientist attributions verified.
</objective>

<tasks>

<task id="02-01-T1">
  <name>Write stops.json with all 50 stops</name>
  <files>Episodio4/assets/data/stops.json</files>
  <action>
    Create JSON object: { "stops": [...50 stop objects...] }.
    Each object has all required fields. Eras in order:
    - ancient (600 BCE – 150 CE): 001-007
    - revolution (1543–1700): 008-013
    - classical (1700–1895): 014-026
    - modern (1895–1945): 027-041
    - contemporary (1945–present): 042-050
    Every stop's next field = following stop's id. First stop prev=null, last stop next=null.
  </action>
  <verify>
    python3 -c "import json; d=json.load(open('Episodio4/assets/data/stops.json'));
    stops=d['stops']; assert len(stops)==50; assert stops[0]['prev'] is None;
    assert stops[49]['next'] is None;
    [assert stops[i]['next']==stops[i+1]['id'] for i in range(49)]; print('OK')"
  </verify>
  <done>All 50 stops with valid chain. Validation script prints OK.</done>
</task>

</tasks>
